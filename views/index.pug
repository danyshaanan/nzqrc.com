doctype html
html
  head
    style.
      body { font: 14px monospace; text-align: center; margin-top: 200px; }
      body { background-image: url(#{url}); }
      pre { background-color: rgba(255,255,255,0.8); text-align: left; }
  body
    pre.
      #{pre}
